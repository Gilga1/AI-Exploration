# Production dependencies for the Agent Harness FastAPI backend.
# Source of truth is pyproject.toml — regenerate with: pip install -e ".[dev]"

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Config & HTTP
pyyaml>=6.0.2
httpx>=0.27.0

# Agent orchestration
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-mcp-adapters>=0.1.0
deepagents>=0.7.0

# Tools & integrations
firecrawl-py>=4.0.0
fpdf2>=2.8.0

# Data connectors
asyncpg>=0.30.0
redis>=5.0.0

# Observability
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
langfuse>=4.0.0

# Install optional connector backends as needed:
#   pip install -r requirements-azure.txt
#   pip install -r requirements-snowflake.txt
